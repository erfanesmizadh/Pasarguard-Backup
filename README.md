# 🔹 Pasarguard & PG-Node Ultimate Backup

**نسخه:** Final Smart Version  
**نویسنده:** @AVASH_NET  

یک اسکریپت **Bash** کامل برای بکاپ از **Pasarguard** و **PG-Node**، شامل دیتابیس MySQL، فایل‌ها و پوشه‌های حیاتی، با ارسال خودکار به **تلگرام**.

---

## 🌟 ویژگی‌ها

- بکاپ خودکار دیتابیس MySQL (یک یا همه)
- بکاپ پوشه‌ها و فایل‌های مهم:
  - `/opt/pasarguard` → شامل `docker-compose.yml`, `.env`, `backup-config.env` و تمام فایل‌ها
  - `/opt/pg-node` → شامل `docker-compose.yml` و تمام فایل‌ها
  - `/var/lib/pasarguard` → شامل `certs/`, `templates/`, `db-backup/` و همه فایل‌ها
  - `/var/lib/pg-node` → شامل همه فایل‌ها
- پاکسازی بکاپ‌های قدیمی قبل از گرفتن بکاپ جدید
- ارسال خودکار بکاپ به تلگرام
- زمان‌بندی با **Cron** (1، 2، 3، 4، 6، 8، 12، 24 ساعت)
- گزارش کامل لاگ در `/var/log/pasarguard-backup.log`
- نصب خودکار وابستگی‌ها (`whiptail`, `zip`, `curl`, `docker.io`)

---

## ⚡ نصب و راه‌اندازی سریع

برای نصب و اجرای اسکریپت کافی است:

```bash
bash -c "$(curl -sL https://raw.githubusercontent.com/erfanesmizadh/Pasarguard-Backup/main/install.sh)"
